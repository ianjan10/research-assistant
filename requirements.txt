# ======================================================================
# Audio Research Assistant - Python dependencies
# Pinned to the versions verified working in the project's .venv.
# Python 3.11+ recommended.
#
# Install:   pip install -r requirements.txt
#
# NOTE on torch: the project's .venv used a CUDA build (torch 2.7.1+cu126).
# The pins below install the CPU build. For GPU, install torch/torchaudio
# from the CUDA index first, e.g.:
#   pip install torch==2.7.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu126
# ======================================================================

# --- Core app / UI ---
streamlit==1.57.0
python-dotenv==1.2.2

# --- Web UI (new FastAPI front end: python run.py --web) ---
fastapi==0.136.1
uvicorn==0.46.0

# --- Data / numerics ---
numpy==2.3.5
scipy==1.17.1
pandas==3.0.3

# --- Embeddings & re-ranking (retrieval engine) ---
sentence-transformers==5.5.0
transformers==4.57.6
torch==2.7.1
torchaudio==2.7.1

# --- Oracle vector database ---
oracledb==4.0.0

# --- PDF reading / parsing ---
PyMuPDF==1.27.2.3
pypdf==6.11.0

# --- LLM providers (selected at runtime via LLM_PROVIDER / ANSWER_PROVIDER) ---
openai==1.109.1
anthropic==0.46.0

# --- Google Gemini (embeddings: EMBEDDING_PROVIDER=google) ---
google-genai==1.75.0

# --- Web search tool ---
requests==2.34.1
beautifulsoup4==4.14.3

# --- Misc utilities ---
tqdm==4.67.3

# ======================================================================
# OPTIONAL: heavy document-ingestion / OCR engines.
# Only needed for re-ingesting PDFs with advanced parsers/OCR
# (PARSER_MODE, ENABLE_DOCLING, ENABLE_MARKER, ENABLE_OCR in .env).
# Uncomment the ones you actually use; they are large downloads.
# ----------------------------------------------------------------------
# OCR fallback for scanned PDFs (only triggers when a page has no text):
# paddleocr==3.5.0
# paddlepaddle==3.3.1
# pytesseract==0.3.13          # also requires the Tesseract binary on PATH
#
# (Docling and Marker were removed — PyMuPDF is the parser for low latency.)
